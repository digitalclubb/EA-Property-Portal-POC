{
  "private": true,
  "name": "ea-property-portal-poc",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "per-env",
    "start:production": "npm run -s serve",
    "start:development": "npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "jest-preset-preact": "^1.0.0",
    "per-env": "^1.0.2",
    "preact-cli": "^3.4.3",
    "preact-render-spy": "^1.2.1"
  },
  "dependencies": {
    "nested-property": "^1.0.1",
    "preact": "^8.5.2",
    "preact-render-to-string": "^4.1.0",
    "preact-router": "^2.5.7",
    "unissist": "^1.4.0",
    "unistore": "^3.4.1"
  },
  "jest": {
    "preset": "jest-preset-preact"
  }
}
