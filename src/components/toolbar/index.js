import { h } from 'preact';
import style from './style';

const Toolbar = ({ id }) => (
    <div class={style.toolbar}>
        last updated
        status: draft
    </div>
);

export default Toolbar;
